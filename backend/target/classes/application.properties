# ==== MySQL Database Config ====
spring.datasource.url=jdbc:mysql://localhost:3306/contactbook_db?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Sidhu@2005

# ==== JPA / Hibernate Config ====
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ==== Server Port ====
server.port=8080

# Add these lines to your existing application.properties

# JWT secret - must be 32+ characters. Change this to your own random string.
# On Render, set this as an environment variable instead of hardcoding it.
jwt.secret=${JWT_SECRET:this-is-a-32-character-secret-key-change-me}

